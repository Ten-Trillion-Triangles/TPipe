--- a/src/main/kotlin/bedrockPipe/BedrockPipe.kt
+++ b/src/main/kotlin/bedrockPipe/BedrockPipe.kt
@@ -200,7 +200,8 @@
             val responseBody = response.body?.let { String(it) } ?: ""
             
             // Extract text using model-specific response parsing
-            extractTextFromResponse(responseBody, modelId)
+            val extractedText = extractTextFromResponse(responseBody, modelId)
+            extractedText
         } 
         catch (e: Exception) 
         {